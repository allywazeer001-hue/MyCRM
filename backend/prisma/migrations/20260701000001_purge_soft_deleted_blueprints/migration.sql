-- This migration's original SQL file was missing from disk (never committed to git).
-- The database's _prisma_migrations table confirms it already ran successfully on
-- 2026-07-01 and its effects are already applied to the real database. Reconstructed
-- here as a no-op placeholder purely so Prisma's migration history can be replayed
-- against the shadow database for future `prisma migrate dev` runs — this file is
-- never re-executed against the real database since it's already marked as finished.
SELECT 1;
